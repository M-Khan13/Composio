# Copy to .env and fill in. .env is gitignored — never commit real keys.

# Composio SDK — https://app.composio.dev  (Settings -> API Keys)
COMPOSIO_API_KEY=

# Google Gemini — https://aistudio.google.com/apikey
GEMINI_API_KEY=

# Optional overrides
GEMINI_MODEL=gemini-2.5-flash
CONCURRENCY=5
